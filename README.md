# Movie Recommender System

A Streamlit app that recommends similar movies using precomputed TMDB movie data.

## Run Locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

Poster images use the TMDB API. Set your API key before running:

```bash
export TMDB_API_KEY="your_api_key_here"
```
